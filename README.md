# cuda-nms-numba
GPU-accelerated Non-Maximum Suppression using CUDA (Numba) — from naive parallel IoU to Matrix NMS.

CSC14116 — Applied Parallel Programming, Topic A4. Team: [TODO — names, student IDs].

## Usage

```bash
pip install -r requirements.txt
python src/cpu_baseline.py --benchmark
pytest tests/
```

See `PROPOSAL_DRAFT.md` for the full project proposal.
