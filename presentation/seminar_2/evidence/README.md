# Seminar 2 evidence

Put only reproducible evidence here:

- full CUDA pytest log;
- benchmark JSON with raw samples and environment metadata;
- batch-size-32 benchmark log/JSON;
- optional profiler output, screenshots or demo recording.

Suggested names: `pytest_<gpu>.txt`, `benchmark_<gpu>.json`, and
`batch32_<gpu>.json`. Every slide metric should trace back to one of these
files.
